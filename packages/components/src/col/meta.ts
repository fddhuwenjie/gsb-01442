import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'col',
  exports: ['AkCol'],
  style: 'col',
  category: 'Layout',
  label: 'Col 布局',
  kind: 'component',
  inSidebar: false,
}

export default meta
