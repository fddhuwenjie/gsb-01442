import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'image',
  exports: ['AkImage', 'AkImageViewer'],
  style: 'image',
  category: 'Others',
  label: 'Image 图片',
  kind: 'component',
  inSidebar: false,
}

export default meta
