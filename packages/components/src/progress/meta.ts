import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'progress',
  exports: ['AkProgress'],
  style: 'progress',
  category: 'Data',
  label: 'Progress 进度条',
  kind: 'component',
  inSidebar: false,
}

export default meta
