import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'row',
  exports: ['AkRow'],
  style: 'row',
  category: 'Layout',
  label: 'Row 布局',
  kind: 'component',
  inSidebar: false,
}

export default meta
