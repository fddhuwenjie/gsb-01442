import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'divider',
  exports: ['AkDivider'],
  style: 'divider',
  category: 'Layout',
  label: 'Divider 分割线',
  kind: 'component',
}

export default meta
