import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'text',
  exports: ['AkText'],
  style: 'text',
  category: 'Basic',
  label: 'Text 文本',
  kind: 'component',
  inSidebar: false,
}

export default meta
