import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'space',
  exports: ['AkSpace'],
  style: 'space',
  category: 'Basic',
  label: 'Space 间距',
  kind: 'component',
  inSidebar: false,
}

export default meta
