import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'date-picker',
  exports: ['AkDatePicker'],
  style: 'date-picker',
  category: 'Form',
  label: 'DatePicker 日期选择器',
  kind: 'component',
}

export default meta
