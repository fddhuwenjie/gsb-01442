import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'slider',
  exports: ['AkSlider'],
  style: 'slider',
  category: 'Form',
  label: 'Slider 滑块',
  kind: 'component',
  inSidebar: false,
}

export default meta
