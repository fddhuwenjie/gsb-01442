import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'button',
  exports: ['AkButton', 'AkButtonGroup'],
  style: 'button',
  category: 'Basic',
  label: 'Button 按钮',
  kind: 'component',
}

export default meta
