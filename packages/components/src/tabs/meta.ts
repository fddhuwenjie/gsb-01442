import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'tabs',
  exports: ['AkTabs', 'AkTabPane'],
  style: 'tabs',
  category: 'Navigation',
  label: 'Tabs 标签页',
  kind: 'component',
}

export default meta
