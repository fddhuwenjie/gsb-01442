import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'upload',
  exports: ['AkUpload'],
  style: 'upload',
  category: 'Form',
  label: 'Upload 上传',
  kind: 'component',
  inSidebar: false,
}

export default meta
