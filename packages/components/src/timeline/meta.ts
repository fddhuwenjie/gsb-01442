import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'timeline',
  exports: ['AkTimeline', 'AkTimelineItem'],
  style: 'timeline',
  category: 'Others',
  label: 'Timeline 时间线',
  kind: 'component',
  inSidebar: false,
}

export default meta
