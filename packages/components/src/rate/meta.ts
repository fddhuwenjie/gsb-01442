import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'rate',
  exports: ['AkRate'],
  style: 'rate',
  category: 'Form',
  label: 'Rate 评分',
  kind: 'component',
  inSidebar: false,
}

export default meta
