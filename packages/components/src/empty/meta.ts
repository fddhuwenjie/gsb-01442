import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'empty',
  exports: ['AkEmpty'],
  style: 'empty',
  category: 'Data',
  label: 'Empty 空状态',
  kind: 'component',
  inSidebar: false,
}

export default meta
