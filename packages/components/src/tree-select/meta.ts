import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'tree-select',
  exports: ['AkTreeSelect'],
  style: 'tree-select',
  category: 'Others',
  label: 'TreeSelect 树选择',
  kind: 'component',
  inSidebar: false,
}

export default meta
