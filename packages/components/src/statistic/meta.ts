import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'statistic',
  exports: ['AkStatistic', 'AkCountdown'],
  style: 'statistic',
  category: 'Data',
  label: 'Statistic 统计',
  kind: 'component',
  inSidebar: false,
}

export default meta
