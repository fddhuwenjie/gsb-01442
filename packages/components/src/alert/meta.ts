import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'alert',
  exports: ['AkAlert'],
  style: 'alert',
  category: 'Feedback',
  label: 'Alert 提示',
  kind: 'component',
}

export default meta
