import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'tooltip',
  exports: ['AkTooltip'],
  style: 'tooltip',
  category: 'Feedback',
  label: 'Tooltip 提示',
  kind: 'component',
  inSidebar: false,
}

export default meta
