import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'popover',
  exports: ['AkPopover'],
  style: 'popover',
  category: 'Feedback',
  label: 'Popover 弹出框',
  kind: 'component',
  inSidebar: false,
}

export default meta
