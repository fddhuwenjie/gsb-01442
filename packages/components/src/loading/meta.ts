import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'loading',
  exports: ['AkLoading', 'AkLoadingDirective'],
  style: 'loading',
  category: 'Feedback',
  label: 'Loading 加载',
  kind: 'function',
  inSidebar: false,
}

export default meta
