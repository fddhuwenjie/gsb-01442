import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'watermark',
  exports: ['AkWatermark'],
  style: 'watermark',
  category: 'Others',
  label: 'Watermark 水印',
  kind: 'component',
  inSidebar: false,
}

export default meta
