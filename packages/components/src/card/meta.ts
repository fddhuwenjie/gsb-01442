import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'card',
  exports: ['AkCard'],
  style: 'card',
  category: 'Layout',
  label: 'Card 卡片',
  kind: 'component',
}

export default meta
