import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'pagination',
  exports: ['AkPagination'],
  style: 'pagination',
  category: 'Data',
  label: 'Pagination 分页',
  kind: 'component',
}

export default meta
