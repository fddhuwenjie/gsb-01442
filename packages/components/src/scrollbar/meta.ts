import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'scrollbar',
  exports: ['AkScrollbar'],
  style: 'scrollbar',
  category: 'Basic',
  label: 'Scrollbar 滚动条',
  kind: 'component',
}

export default meta
