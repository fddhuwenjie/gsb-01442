import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'autocomplete',
  exports: ['AkAutocomplete'],
  style: 'autocomplete',
  category: 'Form',
  label: 'Autocomplete 自动补全',
  kind: 'component',
  inSidebar: false,
}

export default meta
