import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'mention',
  exports: ['AkMention'],
  style: 'mention',
  category: 'Form',
  label: 'Mention 提及',
  kind: 'component',
  inSidebar: false,
}

export default meta
