import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'calendar',
  exports: ['AkCalendar'],
  style: 'calendar',
  category: 'Others',
  label: 'Calendar 日历',
  kind: 'component',
  inSidebar: false,
}

export default meta
