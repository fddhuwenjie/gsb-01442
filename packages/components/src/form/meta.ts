import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'form',
  exports: ['AkForm', 'AkFormItem'],
  style: 'form',
  category: 'Form',
  label: 'Form 表单',
  kind: 'component',
}

export default meta
