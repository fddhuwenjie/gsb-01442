import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'transfer',
  exports: ['AkTransfer'],
  style: 'transfer',
  category: 'Others',
  label: 'Transfer 穿梭框',
  kind: 'component',
  inSidebar: false,
}

export default meta
