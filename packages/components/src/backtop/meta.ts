import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'backtop',
  exports: ['AkBacktop'],
  style: 'backtop',
  category: 'Others',
  label: 'BackTop 回到顶部',
  kind: 'component',
  inSidebar: false,
}

export default meta
