import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'switch',
  exports: ['AkSwitch'],
  style: 'switch',
  category: 'Form',
  label: 'Switch 开关',
  kind: 'component',
}

export default meta
