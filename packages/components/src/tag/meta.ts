import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'tag',
  exports: ['AkTag'],
  style: 'tag',
  category: 'Data',
  label: 'Tag 标签',
  kind: 'component',
}

export default meta
