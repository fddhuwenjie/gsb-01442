import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'steps',
  exports: ['AkSteps', 'AkStep'],
  style: 'steps',
  category: 'Navigation',
  label: 'Steps 步骤条',
  kind: 'component',
  inSidebar: false,
}

export default meta
