import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'dialog',
  exports: ['AkDialog'],
  style: 'dialog',
  category: 'Feedback',
  label: 'Dialog 对话框',
  kind: 'component',
}

export default meta
