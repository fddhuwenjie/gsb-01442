import type { ComponentMeta } from '../meta-types'

const meta: ComponentMeta = {
  dir: 'select',
  exports: ['AkSelect', 'AkOption', 'AkOptionGroup'],
  style: 'select',
  category: 'Form',
  label: 'Select 选择器',
  kind: 'component',
}

export default meta
